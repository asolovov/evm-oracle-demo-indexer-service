# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

.idea/

__debug_bin

microservice-template
evm-oracle-demo-indexer-service
bin/

# Template synchronization metadata (only in downstream repos)
.template-version

# Generated protobuf outputs — service-owned codegen lands here at build
# time. NEVER committed. (architecture rule 9)
internal/genproto/

# Generated swagger/HTTP API code (kept locally, not committed)
internal/http/server/
internal/http/models/

# Local docker-compose data
/data/

.env

local
